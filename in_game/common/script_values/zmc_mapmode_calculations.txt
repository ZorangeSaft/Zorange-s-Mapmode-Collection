#####zmc

zmc_used_building_slots_percentage = { #building cap mapmode
  value = 1
  subtract = {
    value = modifier:free_building_levels
    subtract = total_building_levels
	subtract = num_civil_constructions
    divide = {
      value = modifier:free_building_levels
      min = 1
    }
  }
}

zmc_free_building_slots = { #building cap mapmode
	value = 0
	value = modifier:free_building_levels
    subtract = total_building_levels
	subtract = num_civil_constructions
}

zmc_used_building_slots_percentage_big = { #building cap mapmode
	value = zmc_used_building_slots_percentage
	multiply = 100
}

zmc_total_building_levels = { #building cap mapmode #building levels mapmode
	value = total_building_levels
	add = num_civil_constructions
}



zmc_free_building_levels = { #free building levels mapmode
	value = modifier:free_building_levels
}



zmc_buildings_under_construction = { #number of buildings under construction mapmode
	value = 0
	every_buildings_in_location = {
		add = building_levels_under_construction
	}
}




zmc_pop_growth_percentage = { #pop growth mapmode
  value = modifier:local_population_growth
  multiply = 100
}





zmc_local_monthly_development = { #dev growth mapmode
  value = modifier:local_monthly_development
  if = {
  	limit = { exists = owner }
  	add = owner.modifier:global_monthly_development
  }
  multiply = {
    value = 1
    add = modifier:local_monthly_development_modifier
  multiply = 100
  }
}





zmc_time_until_100_development = { #time until 100 dev mapmode
  value = 100 #100 development
  subtract = development #subtract current development
  divide = zmc_time_until_100_development_sub_value
  divide = 12 #convert from months to years
}

zmc_time_until_100_development_sub_value = {
  value = modifier:local_monthly_development
  if = {
  	limit = { exists = owner }
  	add = owner.modifier:global_monthly_development
  }
  multiply = {
    value = 1
    add = modifier:local_monthly_development_modifier
  }
  min = 0.0001 #to avoid dividing by 0
}





zmc_pop_cap_percentage = { #pop cap percentage mapmode
  value = population
  multiply = 1000 #so it matches the multiplied pop cap
  divide = zmc_pop_cap
}

zmc_pop_cap = {
  value = modifier:local_population_capacity
  multiply = 1000 #1000 #for 1 pop being 1000 people
  multiply = zmc_pop_cap_temp_value
}

zmc_pop_cap_temp_value = {
  value = modifier:local_population_capacity_modifier
  add = 1 #because in game these modifiers are defined as 0.1 meaning 110%
}

zmc_population = {
  value = population
  multiply = 1000 #for 1 pop being 1000 people
}

zmc_province_population = {
  value = province_definition.population
  multiply = 1000 #for 1 pop being 1000 people
}

zmc_area_population = {
	value = area.population
	multiply = 1000 #for 1 pop being 1000 people
}

zmc_region_population = {
	value = region.population
	multiply = 1000 #for 1 pop being 1000 people
}

zmc_sub_continent_population = {
	value = sub_continent.population
	multiply = 1000 #for 1 pop being 1000 people
}

zmc_continent_population = {
	value = continent.population
	multiply = 1000 #for 1 pop being 1000 people
}

zmc_world_population = {
	value = global_var:zmc_world_population
}






zmc_control_times_market_access = { #control times market access mapmode
  value = local_control
  multiply = market_access
}





zmc_location_tax_base_per_population = { #wealth per population mapmode, basically gdp/capity
  value = location_tax_base #wealth, but modified by control (tried to undo it by dividing by control, but if control is 0 this doesnt work)
  divide = population
}

zmc_province_tax_base_per_population = {
	province = {
        every_location_in_province = {
            limit = { is_ownable = yes }
            add = location_tax_base
        }
    }
    divide = province.population
}

zmc_area_tax_base_per_population = {
	area = {
        every_location_in_area = {
            limit = { is_ownable = yes }
            add = location_tax_base
        }
    }
    divide = area.population
}

zmc_region_tax_base_per_population = {
	region = {
        every_location_in_region = {
            limit = { is_ownable = yes }
            add = location_tax_base
        }
    }
    divide = region.population
}

zmc_sub_continent_tax_base_per_population = {
	sub_continent = {
        every_location_in_sub_continent = {
            limit = { is_ownable = yes }
            add = location_tax_base
        }
    }
    divide = sub_continent.population
}

zmc_continent_tax_base_per_population = {
	continent = {
		every_location_in_continent = {
			limit = { is_ownable = yes }
			add = location_tax_base
		}
	}
	divide = continent.population
}

zmc_world_tax_base_per_population = {
	value = global_var:zmc_world_tax_base_per_population_var
}





zmc_local_production_efficiency = { #production efficiency mapmode
  value = modifier:local_production_efficiency
  add = owner.modifier:global_production_efficiency
  multiply = 100
}





zmc_local_max_literacy = { #max literacy mapmode
	every_pop = {
		limit = { pop_size > 0 }
		add = {
			if = {
				limit = { pop_type = pop_type:nobles }
				add = prev.modifier:local_nobles_max_literacy
				add = owner.modifier:global_nobles_max_literacy
			}
			else_if = {
				limit = { pop_type = pop_type:clergy }
				add = prev.modifier:local_clergy_max_literacy
				add = owner.modifier:global_clergy_max_literacy
			}
			else_if = {
				limit = { pop_type = pop_type:burghers }
				add = prev.modifier:local_burghers_max_literacy
				add = owner.modifier:global_burghers_max_literacy
			}
			else_if = {
				limit = { pop_type = pop_type:burghers }
				add = prev.modifier:local_burghers_max_literacy
				add = owner.modifier:global_burghers_max_literacy
			}
			else_if = {
				limit = { pop_type = pop_type:laborers }
				add = prev.modifier:local_laborers_max_literacy
				add = owner.modifier:global_laborers_max_literacy
			}
			else_if = {
				limit = { pop_type = pop_type:soldiers }
				add = prev.modifier:local_soldiers_max_literacy
				add = owner.modifier:global_soldiers_max_literacy
			}
			else_if = {
				limit = { pop_type = pop_type:peasants }
				add = prev.modifier:local_laborers_max_literacy
				add = owner.modifier:global_peasants_max_literacy
			}
			else_if = {
				limit = { pop_type = pop_type:tribesmen }
				add = prev.modifier:local_tribesmen_max_literacy
				add = owner.modifier:global_tribesmen_max_literacy
			}
			else_if = {
				limit = { pop_type = pop_type:slaves }
				add = prev.modifier:local_slaves_max_literacy
				add = owner.modifier:global_slaves_max_literacy
			}
			add = prev.modifier:local_max_literacy
			add = owner.modifier:global_max_literacy
			multiply = {
				add = pop_size
				multiply = 1000
				divide = prev.population
			}
			divide = 1000
		}
	}
}





zmc_control_difference = {
  value = max_control
  subtract = local_control
  multiply = -100
}





zmc_location_size_impact = {
	value = location_size #occupation_time #modifier:occupation_time
}





zmc_closeness_to_equator_impact = {
	value = modifier:location_closeness_to_equator_impact
}





zmc_location_tax_base = {
	value = location_tax_base
}

zmc_province_tax_base = {
	value = province.province_tax_base #province_tax_base #location_tax_base
}

zmc_area_tax_base = {
	area = {
		every_location_in_area = {
			limit = { is_ownable = yes }
			add = location_tax_base
		}
	}
}

zmc_region_tax_base = {
	region = {
		every_location_in_region = {
			limit = { is_ownable = yes }
			add = location_tax_base
		}
	}
}

zmc_sub_continent_tax_base = {
	sub_continent = {
		every_location_in_sub_continent = {
			limit = { is_ownable = yes }
			add = location_tax_base
		}
	}
}

zmc_continent_tax_base = { #too laggy, crashes the game every time you try and use it
	continent = {
		every_location_in_continent = {
			limit = { is_ownable = yes }
			add = location_tax_base
		}
	}
}

zmc_world_tax_base = {
	value = global_var:zmc_world_tax_base
}





#zmc_real_world_size = { #captjazz formula https://discord.com/channels/1221948891704856757/1221948893689020498/1529584975886680065
#	value = -694.65
#	add = zmc_real_world_size_linear_part
#	add = zmc_real_world_size_quadratic_part
#}

#zmc_real_world_size_linear_part = {
#	value = 70.253
#	multiply = location_size
#}

#zmc_real_world_size_quadratic_part = {
#	value = 2.2549
#	multiply = location_size
#	multiply = location_size
#}



#zmc_real_world_size = { #formula based on table with relevant values, that i gave to gpt5.3 codex in vscode
#	value = zmc_real_world_size_location_component
#	multiply = zmc_real_world_size_equator_component
#}

#zmc_real_world_size_location_component = {
#	value = location_size
#	pow = 1.912
#	multiply = 3.19
#}

#zmc_real_world_size_equator_component = {
#	value = 2.617
#	pow = zmc_equator_measurer_script_value
#}



zmc_real_world_size = { #sligthly more complicated formula, also from table and such
	value = 2.6565
	multiply = zmc_real_world_size_location_component
	multiply = zmc_real_world_size_equator_component
}

zmc_real_world_size_location_component = {
	value = location_size
	pow = 1.9533
}

zmc_real_world_size_equator_component = {
	value = 2.7183 #2.7182818285 #eulers number
	pow = zmc_real_world_size_equator_exponent
}

zmc_real_world_size_equator_exponent = {
	value = zmc_real_world_size_equator_linear_term
	add = zmc_real_world_size_equator_quadratic_term
}

zmc_real_world_size_equator_linear_term = {
	value = zmc_equator_measurer_script_value
	multiply = 1.6330
}

zmc_real_world_size_equator_quadratic_term = {
	value = zmc_real_world_size_z_squared
	multiply = -0.8312
}

zmc_real_world_size_z_squared = {
	value = zmc_equator_measurer_script_value
	multiply = zmc_equator_measurer_script_value
}

#margin of error according to gpt5.3 codex of the above complicated formula
#Mean absolute percent error (MAPE): 2.8784%
#Median absolute percent error: 1.6712%
#90th percentile absolute percent error: 3.9979%
#95th percentile absolute percent error: 5.1193%
#99th percentile absolute percent error: 14.8097%
#Mean signed percent bias: +0.5902% (slight overprediction on average)
#Median signed percent bias: -0.1328%
#MAE: 113.67 km²
#RMSE: 649.91 km²
#R²: 0.982199





zmc_equator_measurer_script_value = {
	value = modifier:zmc_equator_measurer_modifier
}





zmc_population_density_real_world = {
	value = zmc_population
	divide = zmc_real_world_size #zmc_location_size_impact
}





zdv_labor_surplus_location = {
    value = this.population
    multiply = {
        value = 1
        subtract = modifier:local_clergy_desired_pop_scaled
        subtract = modifier:local_nobles_desired_pop_scaled
        if = {
            limit = {
                NOT = {
                    location_rank = location_rank:rural_settlement
                }
            }
            owner = {
                subtract = modifier:global_nobles_city_desired_pop_scaled
                subtract = modifier:global_clergy_city_desired_pop_scaled
            }
        }
    }
    every_buildings_in_location = {
        subtract = building_employment_size_amount
        if = {
            limit = {
                building_levels_under_construction > 0
            }
            subtract = {
                value = building_levels_under_construction
                building_type = {
                    multiply = employment_size
                }
            }
        }
    }
    subtract = modifier:local_nobles_desired_pop
    subtract = modifier:local_clergy_desired_pop
    subtract = modifier:local_burghers_desired_pop
    subtract = modifier:local_laborers_desired_pop
    subtract = modifier:local_soldiers_desired_pop
}

zdv_labor_surplus_location_tooltip = {
	value = zdv_labor_surplus_location
	multiply = 1000
}