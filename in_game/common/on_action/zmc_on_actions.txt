#####zmc

on_game_start = { #for new saves
	on_actions = {
		zmc_on_game_start
	}
}

weather_monthly_pulse = {
	on_actions = {
		zmc_yearly
		zmc_on_game_start #for adding to existing saves
	}
}

zmc_on_game_start = {
	trigger = { NOT = { has_global_variable = zmc_setup_done } }
	effect = { zmc_setup_once = yes } #includes zmc_yearly setup
}

zmc_yearly = {
	trigger = { current_month = 1 }
	effect = { zmc_setup_yearly = yes }
}
